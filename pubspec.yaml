name: pie_timer
description: Customizable Timer package with a pie animation. This package can render a circular countdown timer with animated pie-shaped progress.
version: 1.4.1
homepage: https://github.com/cavdarfurkan/pie_timer

environment:
  sdk: ">=2.17.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
